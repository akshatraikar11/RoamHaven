// Call OpenAI API to get real Jarvis response
const jarvisResponse = await fetch("/api/jarvis", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ message: userText }),
})
  .then(res => res.json())
  .then(data => data.reply)
  .catch(() => "Sorry, I couldn't understand that.");
