<style>
  .search-btn {
    background-color: #ff385c;
    color: #fff;
    border-radius: 25px;
    padding: 0 1rem 0 1rem;
    border: none;
    font-weight: 500;
  }

  .search-btn:hover {
    background-color: #ff385c;
    color: #fff;
    opacity: 0.9;
  }

  .search-btn i {
    display: inline;
    margin: 0.5rem;
  }

  .search-inp {
    border-radius: 25px;
    padding: 0.5rem 3rem 0.5rem 3rem;
    font-size: 1rem;
  }
</style>
<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/listings">
      <div class="brand-container">
        <img src="/images/logo.svg?v=4" alt="RoamHaven Logo" class="brand-logo">
        <span class="brand-text">RoamHaven</span>
      </div>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <!-- Left side - empty for now -->
      <div class="navbar-nav">
        <!-- Explore link removed as it's redundant with Brand Logo or we can keep it if needed, but request said replace -->
      </div>

      <!-- Centered Search Pill -->
      <div class="navbar-search-container">
        <div class="search-bar-pill shadow-sm">
          <div class="search-inputs">
            <div class="search-item flex-grow-1">
              <span class="search-label">Where</span>
              <input class="search-input" type="text" placeholder="Search destinations">
            </div>
          </div>
          <button class="search-icon-btn">
            <i class="fa-solid fa-magnifying-glass"></i>
          </button>
        </div>
      </div>

      <!-- Right side buttons -->
      <div class="navbar-nav ms-auto gap-2">
        <a class="nav-link nav-btn" href="/listings/new">Your Home</a>
        <% if(!currUser) { %>
          <a class="nav-link nav-btn" href="/signup">Sign Up</a>
          <a class="nav-link nav-btn" href="/login">Log in</a>
          <% } %>
            <% if(currUser) { %>
              <a class="nav-link nav-btn" href="/logout">Log Out</a>
              <% } %>
      </div>
    </div>
  </div>





</nav>